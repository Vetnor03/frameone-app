# Shop image assets

Place the hero image provided by design here as:

- `hero-top.jpg` (recommended minimum width: 1800px)

The shop hero section (`app/shop/page.tsx`) will render this image with a left-edge fade to white behind the text.
