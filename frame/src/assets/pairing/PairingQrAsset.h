#pragma once
#include <Arduino.h>

namespace PairingQrAsset {
// Monochrome QR bitmap for pairing screen (1-bit, black pixels set).
// Replace this data with the task-provided QR source bitmap bytes.
static const uint8_t kQrMonoBitmap[] PROGMEM = {
  0x00
};
}
