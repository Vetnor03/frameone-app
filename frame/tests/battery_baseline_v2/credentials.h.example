#pragma once

// LOCAL-ONLY VALUES. Copy to credentials.h and keep it out of git.
#define TEST_WIFI_SSID "YOUR_WIFI_SSID"
#define TEST_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define TEST_DEVICE_TOKEN "YOUR_DEVICE_TOKEN"
